# OCR pipeline (pointer)

The OCR/extraction pipeline documentation lives in the **extraction repository**:
`docs/ocr-pipeline.md` in <https://github.com/colinpriest/lloyds-reserve-stress-testing>.
Its section 10.3 is the canonical statement of the PYD source hierarchy, including the
sign-disagreement exception under which the gross provisions movement overrides the
deterministic triangle.

This file was previously a full copy. A duplicate outside any synchronisation
machinery is how a correction lands in one repository and not the other -- the exact
failure that has recurred on this project -- so the copy is replaced by this pointer.
